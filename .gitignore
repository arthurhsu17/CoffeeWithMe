# dependencies
node_modules
node_modules.nosync

# production build
build

# local env files (may contain API keys)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# macOS
.DS_Store

# local Claude Code config (machine-specific paths)
.claude/settings.local.json
.claude/launch.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
